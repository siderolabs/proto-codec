# commit to be tagged for the new release
commit = "HEAD"

project_name = "proto-codec"
github_repo = "siderolabs/proto-codec"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
